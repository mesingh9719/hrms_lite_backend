# No environment variables are required for this assignment deployment.
# The backend is configured with:
# - ALLOWED_HOSTS = ['*']
# - CORS_ALLOW_ALL_ORIGINS = True
# - SQLite local: backend/db.sqlite3, Vercel: /tmp/db.sqlite3
